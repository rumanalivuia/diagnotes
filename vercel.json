{
  "buildCommand": "npm run build --workspace web",
  "outputDirectory": "web/dist",
  "rewrites": [
    { "source": "/neonauth/(.*)", "destination": "/api" },
    { "source": "/api/(.*)", "destination": "/api" },
    { "source": "/(.*)", "destination": "/index.html" }
  ],
  "functions": {
    "api/index.js": {
      "includeFiles": "server/**"
    }
  }
}